/*
3. Entre com um nome e imprima o nome somente se a primeira letra
   do nome for ‘a’ (maiúscula ou minúscula).
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, scanf, fgets)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main()  // Função principal
{
    char nome[50];  // Declara um vetor de caracteres para armazenar o nome (máx. 49 + '\0')

    // Solicita o nome do usuário
    printf("Digite um nome: ");
    fgets(nome, sizeof(nome), stdin);  // Lê o nome digitado (aceita espaços e inclui '\n' no final)

    // Verifica se a primeira letra é 'a' ou 'A'
    if (nome[0] == 'a' || nome[0] == 'A')  // Condição: compara o primeiro caractere da string
    {
        printf("\nO nome digitado comeca com 'A': %s", nome);  // Exibe o nome
    }
    else  // Caso a primeira letra não seja 'a' nem 'A'
    {
        printf("\nO nome NAO comeca com 'A'.\n");  // Mensagem de aviso
    }

    system("pause");  // Mantém o console aberto (usado no Windows)
    return 0;         // Finaliza o programa com sucesso
}
