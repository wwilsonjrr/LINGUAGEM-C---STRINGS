/*
15. Escreva um programa para converter uma cadeia de caracteres de letras maiúsculas
    em letras minúsculas.
    Dica: some 32 aos caracteres cujo código ASCII está entre 65 e 90.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, fgets)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main(void)
{
    char texto[100];  // Vetor para armazenar até 99 caracteres + '\0'
    int i = 0;        // Índice para percorrer o vetor

    // Solicita ao usuário que digite uma palavra ou frase
    printf("Digite um texto em letras maiusculas: ");
    fgets(texto, sizeof(texto), stdin);  // Lê a string (aceita espaços)

    // Percorre cada caractere da string até encontrar o final ('\0')
    while (texto[i] != '\0')
    {
        // Verifica se o caractere atual está entre 'A' (65) e 'Z' (90) na tabela ASCII
        if (texto[i] >= 65 && texto[i] <= 90)
        {
            texto[i] = texto[i] + 32;  // Converte para minúscula somando 32
        }

        i++;  // Passa para o próximo caractere
    }

    // Exibe a string convertida
    printf("\nTexto convertido para minusculas: %s\n", texto);

    system("pause");  // Mantém o console aberto (Windows)
    return 0;         // Finaliza o programa corretamente
}
