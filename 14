/*
14. Faça um programa que leia uma palavra (máximo de 50 letras)
    e some 1 no valor ASCII de cada caractere da palavra.
    Imprima a string resultante.
*/

#include <stdio.h>   // Biblioteca padrão para entrada e saída (printf, fgets)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main()  // Função principal
{
    char palavra[51];  // Vetor que armazena até 50 letras + '\0' (fim da string)
    int i = 0;         // Variável de índice para percorrer os caracteres da string

    // Solicita ao usuário que digite uma palavra
    printf("Digite uma palavra (maximo 50 letras): ");
    fgets(palavra, sizeof(palavra), stdin);  // Lê a string (inclui espaços e '\n' se houver)

    // Percorre a string até encontrar o caractere de fim '\0'
    while (palavra[i] != '\0')
    {
        // Verifica se o caractere atual não é o '\n' (quebra de linha adicionada pelo fgets)
        if (palavra[i] != '\n')
        {
            palavra[i] = palavra[i] + 1;  // Soma 1 ao valor ASCII do caractere atual
        }
        i++;  // Avança para o próximo caractere
    }

    // Exibe o resultado da transformação
    printf("\nString resultante: %s\n", palavra);

    system("pause");  // Mantém o console aberto até o usuário pressionar uma tecla
    return 0;         // Finaliza o programa corretamente
}
