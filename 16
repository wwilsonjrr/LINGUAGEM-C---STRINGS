/*
16. Leia uma cadeia de caracteres e converta todos os caracteres para maiúscula.
    Dica: subtraia 32 dos caracteres cujo código ASCII está entre 97 e 122.
*/

#include <stdio.h>   // Biblioteca padrão de entrada e saída (printf, fgets)
#include <stdlib.h>  // Biblioteca padrão (para system("pause"))

int main(void)
{
    char texto[100];  // Vetor para armazenar até 99 caracteres + o terminador '\0'
    int i = 0;        // Variável de índice para percorrer o vetor

    // Solicita ao usuário que digite uma frase ou palavra
    printf("Digite um texto em letras minusculas: ");
    fgets(texto, sizeof(texto), stdin);  // Lê a string completa (aceita espaços)

    // Percorre cada caractere da string até encontrar o caractere nulo '\0'
    while (texto[i] != '\0')
    {
        // Verifica se o caractere está entre 'a' (97) e 'z' (122)
        if (texto[i] >= 97 && texto[i] <= 122)
        {
            texto[i] = texto[i] - 32;  // Subtrai 32 para transformar em maiúscula
        }

        i++;  // Avança para o próximo caractere
    }

    // Exibe o texto convertido para maiúsculas
    printf("\nTexto convertido para maiusculas: %s\n", texto);

    system("pause");  // Pausa o programa (somente no Windows)
    return 0;         // Encerra o programa corretamente
}
